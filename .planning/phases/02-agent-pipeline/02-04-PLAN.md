---
phase: 02-agent-pipeline
plan: 04
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified:
  - src/app/screens/PipelineScreen.tsx
  - src/app/screens/ClusteringScreen.tsx
  - src/app/routes.tsx
autonomous: false
requirements:
  - AGENT-07

must_haves:
  truths:
    - "User sees all 5 agents with animated status indicators (idle, running, done, error)"
    - "Pipeline triggers from ClusteringScreen after clustering is complete"
    - "Pipeline screen shows sequential agent activation with visual feedback"
    - "After pipeline completes, user can proceed to dashboard"
  artifacts:
    - path: "src/app/screens/PipelineScreen.tsx"
      provides: "Pipeline progress UI with animated agent status indicators"
      exports: ["PipelineScreen"]
    - path: "src/app/routes.tsx"
      provides: "Updated routes with /pipeline path"
      contains: "PipelineScreen"
  key_links:
    - from: "src/app/screens/PipelineScreen.tsx"
      to: "src/app/lib/pipeline.ts"
      via: "Calls runPipeline with all agent functions and status callback"
      pattern: "runPipeline"
    - from: "src/app/screens/ClusteringScreen.tsx"
      to: "/pipeline"
      via: "Navigate to pipeline screen with entryId"
      pattern: "navigate.*pipeline"
    - from: "src/app/screens/PipelineScreen.tsx"
      to: "src/app/lib/agents/*"
      via: "Imports all 5 agent functions"
      pattern: "runSynthesisAgent|runLinkerAgent|runResearchAgent|runActionAgent|runCalendarAgent"
---

<objective>
Create the pipeline progress UI screen and wire it into the app flow: ClusteringScreen triggers the pipeline, PipelineScreen shows animated agent status indicators, and completion navigates to dashboard.

Purpose: This is the user-facing experience of the agent pipeline (AGENT-07). Without it, the pipeline runs invisibly. The animated status indicators are a key UX element showing all 5 agents activating sequentially.
Output: PipelineScreen component, updated ClusteringScreen button, updated routes.
</objective>

<execution_context>
@/Users/avi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/avi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-agent-pipeline/02-01-SUMMARY.md
@src/app/lib/types.ts
@src/app/screens/ClusteringScreen.tsx
@src/app/routes.tsx

<interfaces>
<!-- Pipeline types and agent functions needed -->

From src/app/lib/types.ts:
```typescript
export type AgentName = 'synthesis' | 'linker' | 'research' | 'action' | 'calendar';
export type AgentStatus = 'idle' | 'running' | 'done' | 'error';
export interface PipelineState {
  agents: Record<AgentName, AgentStatus>;
  currentAgent: AgentName | null;
  isComplete: boolean;
  error: string | null;
}
```

From src/app/lib/pipeline.ts:
```typescript
export async function runPipeline(
  entry: Entry,
  agents: PipelineAgents,
  onStatusChange: (state: PipelineState) => void
): Promise<PipelineResult>;
```

Agent imports:
```typescript
import { runSynthesisAgent } from '../lib/agents/synthesisAgent';
import { runLinkerAgent } from '../lib/agents/linkerAgent';
import { runResearchAgent } from '../lib/agents/researchAgent';
import { runActionAgent } from '../lib/agents/actionAgent';
import { runCalendarAgent } from '../lib/agents/calendarAgent';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PipelineScreen with animated agent status indicators</name>
  <files>src/app/screens/PipelineScreen.tsx</files>
  <action>
Create `src/app/screens/PipelineScreen.tsx` — the pipeline progress UI.

**Component behavior:**
1. Read `entryId` from URL search params (same pattern as ClusteringScreen)
2. Load entry from localStorage via `getEntry(entryId)`
3. On mount, call `runPipeline(entry, agents, onStatusChange)` where agents is an object mapping all 5 agent functions
4. Track pipeline state via `useState<PipelineState>` updated by the onStatusChange callback
5. On completion, show a "View Dashboard" button that navigates to `/dashboard`
6. On error, show the error message with option to retry

**UI Layout — match existing app style (font-['Lora'] headers, font-['DM_Sans'] body, #F7F5F0 bg, #0D0D0D text):**

- Full-page centered layout with heading "Processing your thoughts..."
- 5 agent cards arranged vertically, each showing:
  - Agent name (human-friendly: "Synthesis", "Pattern Linker", "Research", "Action Planner", "Calendar")
  - Brief description of what it does
  - Status indicator (animated bullet/icon):
    - `idle`: gray dot, muted text
    - `running`: pulsing/spinning animated indicator (use motion from "motion/react"), active text color
    - `done`: green checkmark with subtle scale-in animation
    - `error`: red X mark
  - Use motion.div for enter/exit animations on status changes

- The currently running agent should be visually prominent (slightly larger card, highlighted border)
- Use the project's color palette: #C8D5B0 (green), #F5E642 (yellow), #F0D5D0 (pink), #F5C4A1 (orange)
- Each agent card gets a distinct accent color from the palette

- After pipeline completes: show success message + "View Dashboard" button (same styling as ClusteringScreen's "Create My Dashboard" button — yellow background, dark text, rounded-full)

**Icons from lucide-react:**
- Synthesis: Sparkles
- Linker: Link2
- Research: Search
- Action: Lightbulb
- Calendar: Calendar

Do NOT use any components not already in the project. Use motion (framer-motion), lucide-react, shadcn/ui Card/Button components.
  </action>
  <verify>
    <automated>cd /Users/avi/.superset/worktrees/Hackathon/avi/illustrious-brook && npx tsc --noEmit src/app/screens/PipelineScreen.tsx 2>&1 | head -20</automated>
  </verify>
  <done>PipelineScreen shows 5 agent cards with animated status indicators (idle/running/done/error). Running agent has pulsing animation. Completed agents show green checkmark. Pipeline auto-starts on mount, navigates to dashboard on completion.</done>
</task>

<task type="auto">
  <name>Task 2: Wire pipeline into app flow (routes + ClusteringScreen trigger)</name>
  <files>src/app/routes.tsx, src/app/screens/ClusteringScreen.tsx</files>
  <action>
**File 1: `src/app/routes.tsx`**

Add the /pipeline route:
1. Import PipelineScreen: `import { PipelineScreen } from "./screens/PipelineScreen";`
2. Add route entry: `{ path: "/pipeline", Component: PipelineScreen }`

**File 2: `src/app/screens/ClusteringScreen.tsx`**

Change the "Create My Dashboard" button to trigger the pipeline instead of going directly to dashboard:
1. Find the Button with `onClick={() => navigate("/dashboard")}` (around line 487)
2. Change it to: `onClick={() => navigate(`/pipeline?entryId=${entryId || ''}`)}`
3. Change button text from "Create My Dashboard" to "Analyze My Thoughts"
4. Keep all existing styling (rounded-full, yellow background, etc.)

The entryId is already available in ClusteringScreenContent via `searchParams.get("entryId")`.

If no entryId is present (mock mode), still navigate to /pipeline — PipelineScreen should handle missing entryId gracefully (show an error or use a placeholder).
  </action>
  <verify>
    <automated>cd /Users/avi/.superset/worktrees/Hackathon/avi/illustrious-brook && npx tsc --noEmit src/app/routes.tsx src/app/screens/ClusteringScreen.tsx 2>&1 | head -20</automated>
  </verify>
  <done>/pipeline route exists. ClusteringScreen's button navigates to /pipeline with entryId. The flow is now: /voice -> /cluster -> /pipeline -> /dashboard.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete pipeline flow end-to-end</name>
  <files>n/a</files>
  <action>
Human verifies the complete agent pipeline flow works visually and functionally.

What was built: Complete agent pipeline with 5 sequential agents and animated pipeline progress UI. Flow: voice recording -> clustering -> pipeline (5 agents process sequentially with status indicators) -> dashboard.

How to verify:
1. Start the dev server: `npm run dev` (or `pnpm dev`)
2. Go through the voice recording flow (or navigate directly to /cluster)
3. Click "Analyze My Thoughts" on the clustering screen
4. Observe the pipeline screen:
   - All 5 agents should appear with idle status
   - Agents should activate one by one (idle -> running -> done)
   - Running agent should have a pulsing/spinning animation
   - Completed agents should show a green checkmark
   - If any agent fails, it should show an error indicator
5. After all agents complete, click "View Dashboard"
6. Verify the app doesn't crash and the flow feels smooth

Note: Requires VITE_OPENAI_API_KEY in .env for agents to work. If Exa key missing, ResearchAgent should gracefully return empty results.
  </action>
  <verify>
    <automated>echo "Manual verification — user approves or describes issues"</automated>
  </verify>
  <done>User confirms pipeline flow works: clustering -> pipeline screen with animated agent indicators -> dashboard. Type "approved" or describe issues.</done>
</task>

</tasks>

<verification>
- /pipeline route exists and renders PipelineScreen
- ClusteringScreen "Analyze My Thoughts" button navigates to /pipeline with entryId
- PipelineScreen shows 5 agents with animated status indicators
- Pipeline runs sequentially: synthesis -> linker -> research -> action -> calendar
- Each agent status transitions: idle -> running -> done (or error)
- Completion shows "View Dashboard" button navigating to /dashboard
</verification>

<success_criteria>
- User triggers pipeline from ClusteringScreen and sees all 5 agents activate sequentially with animated status indicators
- Running agent has visible animation (pulsing/spinning)
- Completed agents show checkmark
- Pipeline results are stored (can be verified via localStorage)
- Flow: /cluster -> /pipeline -> /dashboard works end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/02-agent-pipeline/02-04-SUMMARY.md`
</output>
