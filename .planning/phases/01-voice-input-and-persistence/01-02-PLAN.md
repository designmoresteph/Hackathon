---
phase: 01-voice-input-and-persistence
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/screens/VoiceDumpScreen.tsx
  - src/app/screens/WelcomeScreen.tsx
  - src/app/screens/ClusteringScreen.tsx
  - .env
autonomous: false
requirements:
  - VOICE-02
  - VOICE-03
  - VOICE-04

must_haves:
  truths:
    - "User taps record and sees spoken words appear as floating cloud bubbles in real-time"
    - "User can set a custom recording duration before or during recording"
    - "Recording auto-stops when the timer expires and transitions to clustering screen"
    - "ClusteringScreen receives and displays the real transcript words instead of hardcoded mock data"
  artifacts:
    - path: "src/app/screens/VoiceDumpScreen.tsx"
      provides: "Live voice recording screen with real-time cloud bubbles and configurable timer"
      min_lines: 100
    - path: "src/app/screens/ClusteringScreen.tsx"
      provides: "Clustering screen consuming real transcript data"
      min_lines: 100
    - path: ".env"
      provides: "VITE_OPENAI_API_KEY environment variable"
      contains: "VITE_OPENAI_API_KEY"
  key_links:
    - from: "src/app/screens/VoiceDumpScreen.tsx"
      to: "src/app/hooks/useVoiceCapture.ts"
      via: "useVoiceCapture hook for streaming transcription"
      pattern: "useVoiceCapture"
    - from: "src/app/screens/VoiceDumpScreen.tsx"
      to: "src/app/lib/storage.ts"
      via: "saveEntry on recording complete"
      pattern: "saveEntry"
    - from: "src/app/screens/VoiceDumpScreen.tsx"
      to: "/cluster"
      via: "navigate with entry ID in state or search params"
      pattern: "navigate.*cluster"
    - from: "src/app/screens/ClusteringScreen.tsx"
      to: "src/app/lib/storage.ts"
      via: "getEntry to load real transcript"
      pattern: "getEntry"
---

<objective>
Wire real voice capture, configurable duration, and localStorage persistence into the existing VoiceDumpScreen UI, then connect ClusteringScreen to real transcript data.

Purpose: This transforms the static Figma prototype into a working voice journal — the user speaks, sees their words as cloud bubbles, the entry is saved, and the clustering screen works with real data.
Output: Fully functional voice recording flow from tap-to-record through auto-transition to clustering with persisted entry.
</objective>

<execution_context>
@/Users/avi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/avi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-voice-input-and-persistence/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/app/lib/types.ts:
```typescript
export interface TranscriptSegment {
  id: string;
  text: string;
  timestamp: number;
}

export interface Entry {
  id: string;
  createdAt: string;
  durationSeconds: number;
  transcript: TranscriptSegment[];
  fullText: string;
  userName?: string;
}
```

From src/app/lib/storage.ts:
```typescript
export function saveEntry(entry: Entry): void;
export function getEntry(id: string): Entry | null;
export function getAllEntries(): Entry[];
export function deleteEntry(id: string): void;
```

From src/app/hooks/useVoiceCapture.ts:
```typescript
export function useVoiceCapture(options: {
  onSegment: (segment: TranscriptSegment) => void;
  onError?: (error: string) => void;
}): {
  isConnected: boolean;
  isRecording: boolean;
  startRecording: () => Promise<void>;
  stopRecording: () => void;
  error: string | null;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire VoiceDumpScreen with real voice capture, configurable timer, and persistence</name>
  <files>src/app/screens/VoiceDumpScreen.tsx, src/app/screens/WelcomeScreen.tsx, .env</files>
  <action>
**Create `.env`** at project root with placeholder:
```
VITE_OPENAI_API_KEY=your-openai-api-key-here
```

**Modify `VoiceDumpScreen.tsx`** — replace mock transcript with real voice capture while preserving the existing visual design (earthy tones, cloud metaphor, serif fonts, floating bubble animation):

1. **Remove** the mock `transcriptLines` array and the interval that pushes mock lines.

2. **Add state:**
   - `segments: TranscriptSegment[]` — accumulated transcript segments from voice capture
   - `durationSeconds: number` — configurable timer duration, default 600 (10 min). Read initial value from URL search params (`?duration=300`) or default.
   - `isConfiguring: boolean` — whether the duration picker is shown (starts true, recording hasn't begun yet)
   - `entryId: string` — generated via `crypto.randomUUID()` on mount
   - `recordingStartTime: number` — Date.now() when recording begins

3. **Duration configuration UI** (shown before recording starts):
   - Replace auto-start behavior. Show a pre-recording state with duration selector.
   - Render 4 preset buttons: 2 min, 5 min, 10 min, 15 min. Style as pill buttons matching existing earthy aesthetic (rounded-full, Outfit font, #E8E5E0 border, #F5E642 for selected).
   - Also show a small input field to type a custom number of minutes.
   - Large "Start Recording" button below — rounded-full, #F5E642 background, #0D0D0D text, Outfit font uppercase.
   - When user clicks Start: set `isConfiguring = false`, call `startRecording()`, set `recordingStartTime = Date.now()`.

4. **Voice capture integration:**
   - Call `useVoiceCapture({ onSegment, onError })`.
   - `onSegment`: append segment to `segments` state array.
   - `onError`: show error via a small toast/banner at top (use sonner `toast.error()` — already in deps).

5. **Cloud bubbles:** Keep the existing floating cloud bubble animation. Instead of rendering `transcript` (old string[]), render `segments.map(s => s.text)`. Each segment appears as a new floating cloud bubble using the existing motion animation and cloudImage. Preserve all the existing styling (cloud tint colors cycling through sage/blush/peach, DM_Sans font, cloud mask overlay technique).

6. **Timer countdown:** Keep existing countdown logic but use `durationSeconds` state instead of hardcoded 600. When `timeLeft <= 0`:
   - Call `stopRecording()`
   - Build an `Entry` object: `{ id: entryId, createdAt: new Date().toISOString(), durationSeconds, transcript: segments, fullText: segments.map(s => s.text).join(' ') }`
   - Call `saveEntry(entry)` from storage module
   - Navigate to `/cluster?entryId=${entryId}` after a 1-second delay

7. **Manual stop:** Add a "Done" button next to the timer (visible during recording) that triggers the same save-and-navigate flow as timer expiry. Style: rounded-full, #0D0D0D background, white text, Outfit font.

8. **WelcomeScreen.tsx** — pass user name to voice screen: Update `handleStart("voice")` to navigate with the name: `navigate("/voice?name=" + encodeURIComponent(name))`. In VoiceDumpScreen, read `name` from search params and include in the Entry's `userName` field.

IMPORTANT: Preserve ALL existing visual design — the earthy background (#F7F5F0), Lora heading font, Playfair Display timer, cloud image mask technique, floating animation, pulsing mic icon, cancel button, hint text. The ONLY changes are: (a) replacing mock data with real voice, (b) adding duration config pre-screen, (c) adding Done button, (d) saving to localStorage before navigation.
  </action>
  <verify>
    <automated>cd /Users/avi/.superset/worktrees/Hackathon/avi/illustrious-brook && npx tsc --noEmit src/app/screens/VoiceDumpScreen.tsx src/app/screens/WelcomeScreen.tsx 2>&1 || echo "TypeCheck done"</automated>
  </verify>
  <done>VoiceDumpScreen shows duration config before recording, starts real voice capture on tap, displays live transcript segments as floating cloud bubbles, counts down the configured duration, saves Entry to localStorage on stop/expiry, and navigates to /cluster with entryId. WelcomeScreen passes userName.</done>
</task>

<task type="auto">
  <name>Task 2: Connect ClusteringScreen to real transcript data</name>
  <files>src/app/screens/ClusteringScreen.tsx</files>
  <action>
**Modify `ClusteringScreen.tsx`** to load real transcript from localStorage instead of using hardcoded mock data:

1. **Read entryId from URL:** Use `useSearchParams()` to get `entryId` query param. Call `getEntry(entryId)` from storage module.

2. **Replace `allWords` array:** Instead of the hardcoded 18 words, extract words from the entry's transcript segments. Split each segment's text into individual meaningful words/phrases. Simple approach: split `entry.fullText` by common delimiters, filter out stop words (a, the, is, I, to, and, etc.), take unique words. If fewer than 5 words, use the full segment texts as-is.

3. **Replace `initialClusterData`:** For now, keep the same cluster display logic but populate with real words. Create 3-5 clusters by simply distributing the extracted words evenly across clusters with the existing color palette (sage, blush, yellow, peach). Use generic labels like "Group 1", "Group 2" etc. (The real AI clustering comes in Phase 2.)

4. **Fallback:** If no `entryId` param or entry not found in storage, fall back to the existing hardcoded mock data so the screen still works in isolation during development.

5. **Preserve ALL existing visual design** — cloud images, drag-and-drop, SVG connections, color scheme, fonts, animations. Only the DATA source changes.
  </action>
  <verify>
    <automated>cd /Users/avi/.superset/worktrees/Hackathon/avi/illustrious-brook && npx tsc --noEmit src/app/screens/ClusteringScreen.tsx 2>&1 || echo "TypeCheck done"</automated>
  </verify>
  <done>ClusteringScreen reads entryId from URL, loads Entry from localStorage, extracts words from real transcript, distributes them into clusters. Falls back to mock data if no entry found.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete voice recording flow end-to-end</name>
  <files>n/a</files>
  <action>
Human verification of the complete voice-to-clustering flow.
  </action>
  <verify>User confirms the flow works as described below.</verify>
  <done>Voice recording flow approved by user.</done>
  <what-built>Complete voice recording flow: duration config, real-time speech-to-text with floating cloud bubbles, localStorage persistence, and transition to clustering with real transcript data.</what-built>
  <how-to-verify>
    1. Create a `.env` file at project root with your OpenAI API key: `VITE_OPENAI_API_KEY=sk-...`
    2. Run `pnpm dev` and open http://localhost:5173
    3. Enter a name on the welcome screen and click "Voice"
    4. You should see duration presets (2, 5, 10, 15 min) and a Start Recording button
    5. Select a short duration (2 min) and click Start Recording
    6. Allow microphone access when prompted
    7. Speak some thoughts — you should see your words appear as floating cloud bubbles in real-time
    8. Click "Done" or wait for timer to expire
    9. You should auto-navigate to the clustering screen with your spoken words in the clusters
    10. Refresh the browser — go back to /cluster?entryId=... — the data should still be there from localStorage
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues with the recording flow</resume-signal>
</task>

</tasks>

<verification>
- VoiceDumpScreen uses useVoiceCapture hook (not mock data)
- Duration is configurable via preset buttons before recording starts
- Cloud bubbles render TranscriptSegment text in real-time
- Entry is saved to localStorage before navigating to /cluster
- ClusteringScreen reads real transcript from localStorage via entryId param
- Refreshing the browser preserves the entry
- All files pass TypeScript type checking
- Existing UI aesthetic is preserved (earthy tones, cloud metaphor, serif fonts)
</verification>

<success_criteria>
- User can configure recording duration (2/5/10/15 min or custom)
- User speaks and sees real transcribed words as floating cloud bubbles
- Recording stops on timer expiry or manual "Done" tap
- Entry persists in localStorage across page refresh
- ClusteringScreen displays real transcript words in clusters
- Visual design matches existing Figma prototype aesthetic
</success_criteria>

<output>
After completion, create `.planning/phases/01-voice-input-and-persistence/01-02-SUMMARY.md`
</output>
